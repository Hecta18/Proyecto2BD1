# Base de datos PostgreSQL (credenciales fijas para calificación)
# Dentro de Docker Compose el host es "db"; en local usar "localhost".
DATABASE_URL=postgresql://proy2:secret@db:5432/tienda

# API (puerto publicado en docker-compose.yml)
PORT=3000
HOST=0.0.0.0
